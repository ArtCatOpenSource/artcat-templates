<div id="SecondaryColumn"> 
 {{ series.description | textilize }}
</div>

<div id="PrimaryColumn">
  <h1 class="artist-list"><a href="{{series.page_url }}">{{ artist.name | escape }}: {{ series.title | escape }}</a></h1>

  {% for work in works %}
    <div class="thumb-float">
      <div class="thumb-img"><a href="{{ series.page_url }}/{{ work.fragment_url }}"><img src="{{ work.featured_image.legacy_thumb_url }}" alt="{{ work.title | escape }}"/></a></div>
      <p class="thumb-caption"><em>{{ work.title | escape }}</em>, {{ work.date }}</p>
    </div>
  {% endfor %}

  <br class="clear" />

</div>

